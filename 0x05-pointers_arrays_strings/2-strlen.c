#include "main.h"
#include <stdio.h>

/**
 * _strlen - to return length
 *@s: pointer to string to return
 * Return: Always success
 */

int _strlen(char *s)
{
	int nln;

	nln = 0;
	while (s[n] != '\0')
	{
		nln++;
	}
	return (nln);
}
